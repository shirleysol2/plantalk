# Info Log

## 2026-05-15

- 프로필 화면에서 사진 파일을 선택해 프로필 이미지로 저장하도록 변경했다.
- 테스트용 기본 채팅방 데이터를 제거하고, 방이 없는 상태에서 "새로운 계획 노트 만들기"로 시작하도록 변경했다.
- 이전 브라우저 저장소에 남아 있을 수 있는 테스트 채팅방 id(`jeju`, `bangkok`, `osaka`)는 로딩 시 제외하도록 처리했다.
- 채팅방이 있을 때 방 생성 UI를 작은 "채팅방 만들기" 버튼으로 축소했다.
- 두꺼운 검정 라인과 오프셋 그림자를 줄이고, 얇은 보더와 부드러운 그림자 중심으로 스타일을 정리했다.
- 검증: `npm test`, `npm run build`, 로컬 브라우저 화면 확인을 완료했다.

## 2026-05-15 공유 링크와 사용자 코드

- 프로필마다 `userCode`를 생성하고, 기존 저장 프로필에는 로딩 시 새 `userCode`를 보강하도록 변경했다.
- 채팅방마다 `shareCode`를 생성하고 `?room=<shareCode>` 형식의 공유 링크를 만들도록 변경했다.
- 채팅방에는 `createdByUserCode`, `joinedUserCodes`를 저장해 어떤 사용자 코드가 만든 방인지 구분할 수 있게 했다.
- 공유 링크 열기, 방 생성, 링크 복사, 프로필 생성 이벤트를 `plantalk.infoLogs` 로컬 로그에 저장하도록 추가했다.
- 현재 버전은 정적 Netlify 앱과 브라우저 로컬 저장소 기반이라, 다른 기기와 실시간으로 같은 방 데이터를 공유하려면 이후 서버 저장소가 필요하다.
